{
  "name": "http-server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build":"tsc -b",
    "start":"bun dist/index.js",
    "dev":"bun run build && bun start"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "latest",
    "@types/express": "^5.0.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "express": "^5.2.1"
  }
}
